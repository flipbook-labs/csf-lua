local ReplicatedStorage = game:GetService("ReplicatedStorage")

local CSF = require(ReplicatedStorage.Packages.CSF)

-- Type `CSF.` to get autocompletes. Use `type Foo = CSF.` to autocomplete
-- exported types
